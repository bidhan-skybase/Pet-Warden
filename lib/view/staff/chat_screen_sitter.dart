import 'package:flutter/material.dart';

class ChatScreenSitter extends StatelessWidget {
  const ChatScreenSitter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [Text("ChatScreen")],
      ),
    );
  }
}
